# A data structure is a particular way of organizing data in a computer's memory so that it can be used effectively by program code.
# if data is stored in hard disk that is called DBMS
# the part of computer that does computation is called CPU which is mainly proccessor.